mutation EnsureAgent($instanceId: InstanceId!, $extensions: [String!], $name: String) {
  ensureAgent(input: { instanceId: $instanceId, extensions: $extensions, name: $name }) {
    id
    instanceId
    extensions
    name
  }
}
