mutation reserve(
  $node: ID
  $hash: NodeHash
  $title: String
  $reference: String
  $binds: BindsInput
  $instanceId: InstanceId!
) {
  reserve(
    input: {
      node: $node
      hash: $hash
      title: $title
      binds: $binds
      reference: $reference
      instanceId: $instanceId
    }
  ) {
    ...Reservation
  }
}

mutation unreserve($id: ID!) {
  unreserve(input: { reservation: $id }) {
    id
  }
}
