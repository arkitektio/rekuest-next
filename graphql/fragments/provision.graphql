fragment Provision on Provision {
  id
  status
  template {
    id
    node {
      ...Node
    }
    params
  }
}

fragment ProvisionEvent on ProvisionEvent {
  id
  kind
  level
  provision {
    id
  }
  createdAt
}
